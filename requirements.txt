# Linux PrivEsc Toolkit - Python Requirements
# Install with: pip3 install -r requirements.txt

colorama>=0.4.6      # Cross-platform terminal colors
tabulate>=0.9.0      # Table formatting in reports
requests>=2.28.0     # Optional: CVE API lookup (future)